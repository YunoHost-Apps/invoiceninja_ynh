Some post-install information:

* The first user gets created after installing Invoice Ninja
* Other users can be created from inside the application
* Configuration happens in the application itself.
